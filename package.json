{
  "name": "html-animation-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file interactive frontend animation demo prepared for Railway deployment.",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node scripts/check-html.js && node scripts/check-server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
